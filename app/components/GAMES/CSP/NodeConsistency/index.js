/**
 *
 * NodeConsistency
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

function NodeConsistency() {
  return <div />;
}

NodeConsistency.propTypes = {};

export default memo(NodeConsistency);
