/**
 *
 * GradedGameNavigation
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

function GradedGameNavigation() {
  return <div />;
}

GradedGameNavigation.propTypes = {};

export default memo(GradedGameNavigation);
