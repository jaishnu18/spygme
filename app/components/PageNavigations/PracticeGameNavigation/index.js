/**
 *
 * PracticeGameNavigation
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

function PracticeGameNavigation() {
  return <div />;
}

PracticeGameNavigation.propTypes = {};

export default memo(PracticeGameNavigation);
