// import { selectCheckMailPageDomain } from '../selectors';

describe('selectCheckMailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
