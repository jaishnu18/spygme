// import { selectDiscussNewThreadPageDomain } from '../selectors';

describe('selectDiscussNewThreadPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
