// import { selectReadingMaterialPageDomain } from '../selectors';

describe('selectReadingMaterialPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
