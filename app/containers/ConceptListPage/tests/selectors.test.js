// import { selectConceptListPageDomain } from '../selectors';

describe('selectConceptListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
