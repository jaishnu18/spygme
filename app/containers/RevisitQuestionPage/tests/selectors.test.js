// import { selectRevisitQuestionPageDomain } from '../selectors';

describe('selectRevisitQuestionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
