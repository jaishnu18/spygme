// import { selectTestNotAllowedPageDomain } from '../selectors';

describe('selectTestNotAllowedPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
