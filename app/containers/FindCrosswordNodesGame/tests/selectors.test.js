// import { selectFindCrosswordNodesGameDomain } from '../selectors';

describe('selectFindCrosswordNodesGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
