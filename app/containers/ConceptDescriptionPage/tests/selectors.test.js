// import { selectConceptDescriptionPageDomain } from '../selectors';

describe('selectConceptDescriptionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
