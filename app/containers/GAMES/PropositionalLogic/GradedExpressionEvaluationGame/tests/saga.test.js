/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import gradedExpressionEvaluationGameSaga from '../saga';

// const generator = gradedExpressionEvaluationGameSaga();

describe('gradedExpressionEvaluationGameSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
