// import { selectGradedArcConsistencyDomain } from '../selectors';

describe('selectGradedArcConsistencyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
