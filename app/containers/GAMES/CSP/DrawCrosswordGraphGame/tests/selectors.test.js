// import { selectDrawCrosswordGraphGameDomain } from '../selectors';

describe('selectDrawCrosswordGraphGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
