// import { selectFindCrosswordNodesDomain } from '../selectors';

describe('selectFindCrosswordNodesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
