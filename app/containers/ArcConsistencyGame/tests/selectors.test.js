// import { selectArcConsistencyGameDomain } from '../selectors';

describe('selectArcConsistencyGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
