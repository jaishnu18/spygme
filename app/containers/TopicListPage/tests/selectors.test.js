// import { selectTopicListPageDomain } from '../selectors';

describe('selectTopicListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
