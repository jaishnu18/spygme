// import { selectTreeGamePageDomain } from '../selectors';

describe('selectTreeGamePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
