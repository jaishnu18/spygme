// import { selectPlugNPlayPageDomain } from '../selectors';

describe('selectPlugNPlayPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
