// import { selectMyProfilePageDomain } from '../selectors';

describe('selectMyProfilePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
